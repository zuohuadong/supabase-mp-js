# SSH 连接配置
MCP_REMOTE_HOST=root@your-server-ip
MCP_REMOTE_PORT=8000
# MCP_LOCAL_PORT=18080  # 可选，不设置则自动分配

# MCP 端点配置
MCP_BASE_PATH=/mcp

# 如果需要直连 Studio（绕过 Kong 限制），使用以下配置：
# MCP_REMOTE_PORT=3003
# MCP_BASE_PATH=/api/mcp
